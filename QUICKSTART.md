# 快速启动指南

## 🚀 立即开始

### 1. 启动开发服务器

```bash
npm run dev
```

服务器将在 http://localhost:3000 启动（如果端口被占用，会自动使用 3001）

### 2. 访问管理看板

打开浏览器访问：
- **主页**: http://localhost:3000 （自动重定向到 dashboard）
- **看板页面**: http://localhost:3000/dashboard

### 3. 查看功能

#### 监控面板（默认）
- ✅ 4 个实时指标卡片
  - 在线 Worker 总数
  - 全局任务队列长度
  - 忙碌 Worker 数量
  - 平均等待时间
- ✅ Worker 状态表格
  - Worker ID
  - IP 地址
  - 状态（空闲/忙碌中）
  - 当前任务
  - 连接时长（实时更新）

#### 请求日志
点击 "请求日志" 标签查看：
- ✅ 最近 50 条 API 请求
- ✅ 点击任意日志行展开详情
- ✅ 状态码颜色标识
  - 🔴 红色：500+ 错误
  - 🟡 黄色：400-499 客户端错误
  - ⚪ 白色：正常请求
- ✅ 延迟颜色标识
  - 🔴 红色：> 1500ms
  - 🟠 橙色：500-1500ms
  - ⚪ 灰色：< 500ms

### 4. 测试模拟数据

开发环境下，系统会自动加载模拟数据：
- 3 个模拟 Worker（1 个忙碌，2 个空闲）
- 2 个队列中的任务
- 4 条历史日志记录

### 5. API 端点测试

你可以直接访问 API 端点查看数据：

```bash
# 获取系统指标
curl http://localhost:3000/api/dashboard/metrics

# 获取 Worker 状态
curl http://localhost:3000/api/dashboard/workers

# 获取请求日志
curl http://localhost:3000/api/dashboard/logs
```

### 6. 自动刷新

看板会自动刷新数据：
- 📊 指标和 Worker 状态：每 3 秒
- 📝 请求日志：每 1.5 秒

你可以在页面上看到 "上次更新" 时间戳。

## 🎨 UI 特性

### 宫崎骏治愈系风格
- 柔和的色调（蓝色、薄荷绿、蜜桃色、薰衣草）
- 圆角设计
- 平滑的动画过渡
- 柔和的阴影效果

### 响应式设计
- 📱 移动端：指标卡片单列布局
- 💻 桌面端：指标卡片 4 列布局
- 📊 表格：小屏幕支持横向滚动

## 🔧 开发提示

### 修改刷新间隔

编辑 `app/dashboard/page.tsx`：

```typescript
// 修改指标刷新间隔（默认 3000ms）
const interval = setInterval(fetchMetricsAndWorkers, 3000);

// 修改日志刷新间隔（默认 1500ms）
const interval = setInterval(fetchLogs, 1500);
```

### 添加更多模拟数据

编辑 `lib/services/mock-data.ts` 添加更多 Worker、任务或日志。

### 自定义主题颜色

编辑 `tailwind.config.ts`：

```typescript
colors: {
  'soft-blue': '#93c5fd',      // 修改蓝色
  'mint-green': '#6ee7b7',     // 修改绿色
  'peach': '#fdba74',          // 修改桃色
  'lavender': '#c4b5fd',       // 修改薰衣草色
}
```

## 📦 生产部署

### 构建生产版本

```bash
npm run build
npm start
```

### Docker 部署

```bash
# 使用 Docker Compose
docker-compose up -d

# 或手动构建
docker build -t dashboard .
docker run -p 3000:3000 dashboard
```

## 🐛 常见问题

### Q: 端口被占用怎么办？
A: Next.js 会自动尝试下一个端口（3001, 3002...）

### Q: 看不到数据？
A: 确保你在开发环境下，模拟数据会自动加载。检查浏览器控制台是否有错误。

### Q: 如何连接真实的 Worker？
A: 需要实现 WebSocket 服务器。参考 `lib/services/dispatcher.ts` 中的接口。

### Q: 如何禁用模拟数据？
A: 在 `app/api/dashboard/metrics/route.ts` 中移除 `initializeMockData()` 调用。

## 📚 更多信息

查看完整文档：
- [README.md](./README.md) - 完整项目文档
- [设计文档](./.kiro/specs/admin-dashboard/design.md) - 详细设计说明
- [需求文档](./.kiro/specs/admin-dashboard/requirements.md) - 功能需求

## 🎉 开始使用

现在你可以：
1. ✅ 查看实时监控数据
2. ✅ 浏览请求日志
3. ✅ 测试响应式布局
4. ✅ 自定义主题和样式
5. ✅ 集成到你的任务调度系统

祝你使用愉快！ 🌸
